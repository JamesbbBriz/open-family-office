# Open Family Office v0.4.0

Open public/index.html for the new landing page. Open public/demo.html for the six-view interactive demo. Publish only public/ to static hosting. Repository source is intended for GitHub; deploy only public/ as the static website.

Keep the directory together for landing/demo/source-download links. Full local analysis uses the existing Python tools and ofo-* Agent Skills; the browser does not host those agents.

See docs/WEB-DEPLOYMENT.zh-CN.md and docs/VALIDATION-v0.3.md for exact operation and tested boundaries. Private data belongs outside the repository and must never be deployed with the public site.
