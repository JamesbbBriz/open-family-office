Use the `ofo-doctor` project skill. Read `.claude/skills/ofo-doctor/SKILL.md`, then follow its canonical workflow(s). Do not scan unrelated files or bypass the private-workspace rules in `AGENTS.md`.
