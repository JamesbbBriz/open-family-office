Use the `ofo-research` project skill. Read `.claude/skills/ofo-research/SKILL.md`, then follow its canonical workflow(s). Do not scan unrelated files or bypass the private-workspace rules in `AGENTS.md`.
